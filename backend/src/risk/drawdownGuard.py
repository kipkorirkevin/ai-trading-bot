"""drawdownGuard.py — placeholder. Phase 4 of the build plan (Risk Firewall + execution).
The Risk Firewall has final authority: even a 95/100 AI confidence must be
rejected here if daily loss limit / max drawdown / spread / session / etc.
are violated. See spec section 15."""

def check(*args, **kwargs):
    raise NotImplementedError("drawdownGuard.check() not yet implemented")
