"""performance.py — placeholder. Phase 6.
Must support walk-forward + out-of-sample testing — never optimize only
for historical profit (spec section 29, avoid overfitting)."""

def run(*args, **kwargs):
    raise NotImplementedError("performance not yet implemented")
